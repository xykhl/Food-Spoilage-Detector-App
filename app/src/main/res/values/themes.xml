<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Must descend from Theme.AppCompat: MainActivity is an AppCompatActivity so that
         per-app locale switching (AppCompatDelegate.setApplicationLocales) works. -->
    <style name="Theme.FoodSpoilageDetector" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>